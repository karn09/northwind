Building a simple CRUD app, add products and track quantity by unique id.

Built using:
express, mongoose, swig, and sass

How to run:
In main directory, 'npm start'

In browser navigate to 'localhost:3000'
